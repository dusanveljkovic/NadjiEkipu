info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{apiserver}}/auth/login/"
  body:
    type: json
    data: |-
      {
        "username": "tigar",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
