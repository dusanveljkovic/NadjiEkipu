info:
  name: list interests
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiserver}}/interests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
